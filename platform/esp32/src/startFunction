/*
 * startFunction
 * initialise toutes les fonctions de wallplotter et verifie l'état de celui-ci 
 * il appelle donc tout les fonctions de type moteur ou sensor 
 * l'état du robot sera visible via la LED RGB 
 */

void startFunction() {
  // Insérez ici le code à exécuter lorsque la fonction est démarrée
  tournerServo();
}